[package]
name = "rustui"
version = "0.1.0"
edition = "2024"
description = "A small scaffold generator for consistent Rust Ratatui applications."
license = "MIT"

[dependencies]
crossterm = "0.29.0"
ratatui = "0.30.1"
tokio = { version = "1", features = ["sync", "time"] }
